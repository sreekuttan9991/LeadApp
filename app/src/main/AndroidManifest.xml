<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <application
        android:name="com.cm.leadapp.LeadApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeadApp"
        tools:targetApi="31">
        <activity
            android:name="com.cm.leadapp.ui.action.LeadActionsActivity"
            android:exported="false" />
        <activity
            android:name="com.cm.leadapp.ui.onboarding.OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="crm.kbs.edu.in" />
                <data android:pathPattern="/test" />
            </intent-filter>
        </activity>
        <activity android:name="com.cm.leadapp.MainActivity" />
        <activity android:name="com.cm.leadapp.ui.leaddetails.LeadDetailsActivity" />
        <activity android:name="com.cm.leadapp.ui.addlead.AddNewLeadActivity" />
    </application>
</manifest>